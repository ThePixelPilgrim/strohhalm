*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
local.properties
*/build/

# Kotlin
.kotlin/

# Signing secrets (never commit)
keystore.properties
app/keystore.properties
*.jks
*.keystore

# Superpowers brainstorming companion (local mockups)
.superpowers/

# Live tests. These name a real remote and use an operator's own SSH key, so
# they are run from the working tree and never committed.
app/src/test/java/**/Live*Test.kt
